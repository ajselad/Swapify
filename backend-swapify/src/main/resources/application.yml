server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/registration
    username: postgres
    password: ajsiajsi

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true

  # File upload configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
      enabled: true
      file-size-threshold: 2KB

  # REAL EMAIL CONFIGURATION
  mail:
    host: smtp.gmail.com
    port: 587
    username: ajseladuka196@gmail.com
    password: uqem rwfy raib lzzb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          timeout: 20000
          connection-timeout: 20000
          write-timeout: 20000

# JWT Configuration
jwt:
  secret: mySecretKey12345678901234567890123456789012345678901234567890
  expiration: 86400000

# App Configuration
app:
  frontend:
    url: http://localhost:3000
  upload:
    dir: uploads/messages  # Updated for messages
  base:
    url: http://localhost:8081
  file:
    upload-dir: ./uploads/messages
    base-url: http://localhost:8081
    max-size: 10485760  # 10MB in bytes

# CORS Configuration
cors:
  allowed-origins:
    - http://localhost:3000
    - http://localhost:5173

# Logging Configuration
logging:
  level:
    com.example.demo: DEBUG
    org.springframework.web: INFO
    org.springframework.security: INFO